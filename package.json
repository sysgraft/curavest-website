{
  "name": "curavest-website",
  "version": "1.0.0",
  "private": true,
  "description": "Curavest — Fractional CTO services for UK operational businesses. Marketing website.",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check --minimumSeverity error && astro build && node scripts/generate-csp.mjs",
    "build:fast": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "sync": "astro sync",
    "deploy:www": "npm run build && npx wrangler pages deploy dist --project-name curavest-website --branch main"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.4",
    "@fontsource/inter": "^5.3.0",
    "astro": "7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@axe-core/playwright": "^4.13.0",
    "@cloudflare/workers-types": "^5.20260908.1",
    "pa11y": "^10.0.0",
    "playwright": "^1.63.0",
    "typescript": "^5.9.2"
  }
}
